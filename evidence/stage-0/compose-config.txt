name: vv
services:
  keycloak:
    command:
      - start-dev
      - --import-realm
    environment:
      KC_BOOTSTRAP_ADMIN_PASSWORD: change_me_dev_only
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_HEALTH_ENABLED: "true"
    healthcheck:
      test:
        - CMD-SHELL
        - exec 3<>/dev/tcp/localhost/9000 && printf 'GET /health/ready HTTP/1.0\r\n\r\n' >&3 && cat <&3 | grep -q UP
      timeout: 5s
      interval: 10s
      retries: 30
    image: quay.io/keycloak/keycloak:26.0
    networks:
      default: null
    ports:
      - mode: ingress
        target: 8080
        published: "8080"
        protocol: tcp
      - mode: ingress
        target: 9000
        published: "9000"
        protocol: tcp
    volumes:
      - type: bind
        source: /home/claude/vv/infra/keycloak
        target: /opt/keycloak/data/import
        read_only: true
        bind: {}
  minio:
    command:
      - server
      - /data
      - --console-address
      - :9001
    environment:
      MINIO_ROOT_PASSWORD: change_me_dev_only
      MINIO_ROOT_USER: vv_minio
    healthcheck:
      test:
        - CMD-SHELL
        - mc ready local || exit 1
      timeout: 5s
      interval: 10s
      retries: 20
    image: minio/minio:RELEASE.2025-04-08T15-41-24Z
    networks:
      default: null
    ports:
      - mode: ingress
        target: 9000
        published: "9002"
        protocol: tcp
      - mode: ingress
        target: 9001
        published: "9001"
        protocol: tcp
    volumes:
      - type: volume
        source: miniodata
        target: /data
        volume: {}
  postgres:
    environment:
      POSTGRES_DB: vv
      POSTGRES_PASSWORD: change_me_dev_only
      POSTGRES_USER: vv_bootstrap
      VV_APP_PASSWORD: change_me_dev_only
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U vv_bootstrap -d vv
      timeout: 5s
      interval: 5s
      retries: 20
    image: postgres:16-alpine
    networks:
      default: null
    ports:
      - mode: ingress
        target: 5432
        published: "5432"
        protocol: tcp
    volumes:
      - type: volume
        source: pgdata
        target: /var/lib/postgresql/data
        volume: {}
      - type: bind
        source: /home/claude/vv/db
        target: /db
        read_only: true
        bind: {}
      - type: bind
        source: /home/claude/vv/db/init/00_init.sh
        target: /docker-entrypoint-initdb.d/00_init.sh
        read_only: true
        bind: {}
  web:
    build:
      context: /home/claude/vv/apps/web
      dockerfile: Dockerfile
    depends_on:
      keycloak:
        condition: service_started
        required: true
      minio:
        condition: service_started
        required: true
      postgres:
        condition: service_healthy
        required: true
    environment:
      DATABASE_URL: postgres://vv_app:change_me_dev_only@postgres:5432/vv
      OIDC_CLIENT_ID: vv-web
      OIDC_CLIENT_SECRET: change_me_dev_only
      OIDC_ISSUER: http://keycloak:8080/realms/vv
      S3_BUCKET: vv-dev
      S3_ENDPOINT: http://minio:9000
      S3_REGION: at-1
    healthcheck:
      test:
        - CMD-SHELL
        - wget -qO- http://localhost:3000/api/health || exit 1
      timeout: 5s
      interval: 10s
      retries: 20
    networks:
      default: null
    ports:
      - mode: ingress
        target: 3000
        published: "3000"
        protocol: tcp
  worker:
    build:
      context: /home/claude/vv/apps/worker
      dockerfile: Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
        required: true
    environment:
      DATABASE_URL: postgres://vv_app:change_me_dev_only@postgres:5432/vv
      MODEL_API_BASE: ""
      MODEL_API_KEY: ""
      MODEL_PROVIDER: eu-api-stub
      WORKER_CONCURRENCY: "2"
      WORKER_OIDC_CLIENT_ID: vv-worker
      WORKER_OIDC_CLIENT_SECRET: change_me_dev_only
    healthcheck:
      test:
        - CMD-SHELL
        - test -f /tmp/vv-worker-alive && find /tmp/vv-worker-alive -mmin -1 | grep -q .
      timeout: 5s
      interval: 30s
      retries: 5
    networks:
      default: null
networks:
  default:
    name: vv_default
volumes:
  miniodata:
    name: vv_miniodata
  pgdata:
    name: vv_pgdata
